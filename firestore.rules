rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Application data is served by limited API routes using the Admin SDK.
    // Direct SDK/REST reads would bypass those request limits.
    // Firebase Authentication is independent of these database rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
